services:
  harmony:
    image: ${HARMONY_IMAGE:-ghcr.io/nordwestt/harmony-engine:cuda}
    runtime: nvidia
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
